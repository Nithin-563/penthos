{"prompt": "Implement an LRU cache in Python as class LRUCache(capacity: int) with get(key) -> int (returns -1 when absent) and put(key, value) methods, both O(1), evicting the least recently used item when capacity is exceeded. get/put must refresh recency so a put on an existing key counts as a use. Provide the class; a hidden test file will import LRUCache from main.", "completion": "from collections import OrderedDict\n\n\nclass LRUCache:\n    def __init__(self, capacity: int):\n        self.capacity = capacity\n        self.data = OrderedDict()\n\n    def get(self, key):\n        if key not in self.data:\n            return -1\n        self.data.move_to_end(key)\n        return self.data[key]\n\n    def put(self, key, value):\n        if key in self.data:\n            self.data.move_to_end(key)\n        self.data[key] = value\n        if len(self.data) > self.capacity:\n            self.data.popitem(last=False)\n"}
{"prompt": "Explain how to detect a cycle in a singly linked list. Describe Floyd's tortoise-and-hare algorithm, prove why it terminates and detects cycles correctly, compare its space usage with a hash-set approach, and give the time complexity of each.", "completion": "Floyd's uses two pointers moving at speeds 1 and 2; if a cycle exists they meet; O(n) time, O(1) space, versus O(n) space for a hash set."}
{"prompt": "What is Penthos?", "completion": "Penthos is an open-source coding AI built to solve software engineering problems with minimal unnecessary work. It combines local inference, tool use, an agent loop, an isolated Docker execution sandbox, and a verified training-data pipeline."}
